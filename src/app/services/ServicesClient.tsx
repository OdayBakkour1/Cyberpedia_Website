"use client";
import React, { useEffect, useRef } from "react";
import { Zap, Globe, Shield, Lock, Eye, Users, Award, Layers, CheckCircle } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import Link from "next/link";

// ... (all interfaces, services, stats, and logic from original ServicesPage)

const ServicesClient = () => {
  // ... (all logic and JSX from ServicesPage)
  return (
    <div className="min-h-screen bg-slate-950 text-white overflow-hidden">
      {/* ...rest of the JSX from ServicesPage... */}
    </div>
  );
};

export default ServicesClient; 